#Before runing the script install the repository using the following commands

sudo add-apt-repository ppa:strukturag/libheif

sudo apt-get install libheif-examples

sudo apt-get update

#Then make the script executable using the following command:

chmod + x heic_to_jpeg.sh

#Then Run the script 

./heic_to_jpeg.sh
